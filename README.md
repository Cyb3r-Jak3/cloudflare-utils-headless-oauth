# Cloudflare Utils Headless OAuth

Worker for handling OAuth flows in a headless environment, such as a CLI or server-side application for cloudflare-utils.
Read how to use this on [cloudflare-utils docs](http://127.0.0.1:8000/started/#oauth-headless-mode).
